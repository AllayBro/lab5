apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-crm.fullname" . }}
  labels:
    {{- include "chart-crm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "chart-crm.selectorLabels" . | nindent 4 }}

