apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-billing.fullname" . }}
  labels:
    {{- include "chart-billing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "chart-billing.selectorLabels" . | nindent 4 }}

